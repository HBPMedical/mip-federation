Install Ansible

apt install python-setuptools -y
easy_install pip
pip install ansible

H
sudo apt-get install python-pip
pip install ansible


all nodes should have python installed

run : ansible-playbook -i hosts.ini docker-deploy.yaml -c paramiko --ask-pass --ask-become-pass -u {{ username} -vvvv


